# Перепроверка greedy и найденный баг декодирования (6 августа 2026)

**Статус:** прерван на ep4 ради переобучения, но вывод уже получен.

## Что нашли

`Evaluation/run_benchmark_batched.py` вызывал генерацию так:

```python
sampling_params = SamplingParams(max_tokens=max_new_tokens)
```

Параметры декодирования не задавались вообще, а дефолты vLLM —
`temperature=1.0`, `top_p=1.0`, `seed=None`. То есть **весь бенч за всё время
мерил модели чистым сэмплингом из полного распределения, без фиксации seed.**
Отсюда же ~2% расхождения между прогонами одной и той же модели, которые
списывали на недетерминизм.

## Гипотеза, которую проверяли

На странице в ~8к токенов шанс сорваться копится по всем токенам, поэтому
сэмплинг и давал бимодальность «либо эталон, либо ноль». Предполагали, что
greedy уберёт нули и поднимет скор.

## Результаты (greedy, temperature 0)

| модель | greedy | было (сэмплинг) |
|---|---:|---:|
| база Qwen3.5-4B | **0.758** | 0.835 |
| свип ep1 | **0.000** | 0.059 |
| свип ep2 | **0.000** | 0.297 |
| свип ep3 | **0.000** | 0.138 |
| свип ep4, ep5 | не досчитаны | 0.527 / 0.539 |

## Выводы — гипотеза опровергнута в обе стороны

1. **Нули не были артефактом сэмплинга.** Под greedy дообученные чекпоинты дают
   ровно 0.000 на **всех 40** сэмплах. Argmax-траектория модели сломана
   детерминированно: выход 156–456 байт, модель открывает `<style>`, пишет
   полторы строки CSS-комментария и выдаёт EOS. Сэмплинг нули **маскировал** —
   случайность иногда выбивала модель из вырожденного пути, и оттуда брались
   редкие 0.87–0.94. Все ненулевые числа дообученных моделей — удачные броски кубика.
2. **Базе greedy не помог**, а слегка навредил: 0.835 → 0.758. Тезис «сэмплинг
   занижает скор» не подтвердился.
3. Значит ломает **рецепт обучения**, а не измерение.

Проверены и отвергнуты два соседних подозрения:
- **обрезка таргетов при обучении** — `Отбраковано по длине: 0/40`, самый длинный
  сэмпл 15887 токенов при лимите 16384, ничего не резалось;
- **расхождение промптов** — `DRAFTING_PROMPT` обучения и `PROMPT` бенча совпадают
  побайтово, и chat-шаблон тоже: в обоих случаях перед HTML стоит
  `<|im_start|>assistant\n<think>\n\n</think>\n\n`, маскирование лосса это учитывает.

## Что изменено в коде

- флаги `--temperature` (по умолчанию **0 = greedy**), `--top-p`,
  `--repetition-penalty`, `--sampling-seed`;
- режим декодирования пишется в `summary.json` (ключ `decoding`) — без него
  старые и новые прогоны не отличить;
- в `results.csv` добавлены `finish_reason` и `pred_n_tokens`: они различают
  «упёрлись в `max_new_tokens`» и «модель сама выдала ранний EOS» — по одному
  скору это неразличимо.

⚠ **Все числа проекта, снятые до 6 августа, получены сэмплингом T=1.0.**
Сравнивать их с новыми greedy-прогонами напрямую нельзя.

## Где лежит

`checkpoints_exps/d2c-sweep/bench-greedy-{base,ep1,ep2,ep3}/`,
скрипт `experiments/sweep_greedy.sh`.
