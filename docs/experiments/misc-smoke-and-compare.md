# Мелкие прогоны: смоуки и замеры базы

Собрано из артефактов на диске 6 августа. Отдельных описаний у них не было —
восстановлено по `summary.json`, `progress.json` и `time_results.txt`.
Все числа сняты **сэмплингом T=1.0** (баг декодирования, см.
[перепроверку greedy](2026-08-06-greedy-recheck.md)).

## `d2c-compare50` — база на 50 сэмплах Design2Code

| final | block | text | position | color | clip | n_scored | время |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 0.816 | 0.789 | 0.955 | 0.765 | 0.806 | 0.881 | 50/50 | 281 с |

Быстрый замер базовой Qwen3.5-4B для сверки с полным прогоном на 484 сэмплах
(0.810 на 1k-волне). Совпадает в пределах шума. Сохранены `examples/`
и `compare/` (13 + 15 МБ картинок) — на них смотрели глазами и сделали вывод,
что метрика мягче человеческой оценки.

**Занимает 27 МБ, оставлен как эталонный набор картинок.**

## `smoke-merged` — смоук на 20 сэмплах

| final | block | text | position | color | clip | n_scored | время |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 0.827 | 0.840 | 0.932 | 0.757 | 0.795 | 0.890 | 20/20 | 172 с |

Проверка работоспособности харнесса после пересборки образа с `clip_server`
(5 августа): 20/20 оценено, 0 рендер-крашей, `--gpu-memory-utilization 0.9`
не душит CLIP. Именно этот смоук подтвердил, что новый Evaluation с `main`
работает end-to-end. **5.9 МБ, оставлен.**

## `d2c-compare-base` и `d2c-ctx32k-base` — провалившиеся прогоны

Оба: `n_scored = 0`, все метрики `nan`, `summary.json` пустой по сути,
`examples/` пуст, `progress.json` отсутствует. То есть прогон падал до того,
как хоть один сэмпл был оценён.

`d2c-ctx32k-base` — попытка замерить базу на контексте 32k. Осталось наблюдение
(из рабочих заметок, не из этих файлов), что база на 32k уходит в разнос,
но **числами это не подтверждено** — прогон не дал ни одной оценки.

**Удалены 6 августа** — в них не было ничего, кроме пустых каталогов.
Если гипотеза про длину контекста понадобится, прогон надо ставить заново.
