# Июльская линия: первые прогоны на WebCode2M (30–31 июля 2026)

**Статус:** закрыта, перекрыта пилотом. Веса удалены 6 августа, артефакты сравнения сохранены.

## Что это было

Первые попытки дообучить Qwen3-VL-4B на WebCode2M — до того, как появился
регулярный бенч Design2Code. Сравнение делалось скриптом `compare.py` глазами,
`summary.json` **по дообученным прогонам** не существует, поэтому чисел бенча
по ним нет.

### Единственный сохранившийся замер этого периода

В рабочем дереве репозитория лежит `Evaluation/bench_results/` (не под git,
закрыт `.gitignore`) — прогон **базовой** модели от 30 июля, Design2Code-484,
484/484 оценено, ноль исключённых:

| block | text | position | color | clip | final | final_arith |
|---:|---:|---:|---:|---:|---:|---:|
| 0.818 | 0.957 | 0.736 | 0.751 | 0.863 | **0.804** | 0.825 |

⚠ Ключа `decoding` в `summary.json` нет — значит снято **сэмплингом T=1.0**
(до 6 августа параметры декодирования не задавались вовсе). С greedy-числами
не сравнимо. С прогонами базы из пилота (0.810 на волне 1k, 0.826 на 3k) это
согласуется по порядку величины: расхождение — разброс сэмплинга.

## Где лежит

`/mnt/storage-1/Screenshot2Code/Data/runs/`

| путь | что | статус |
|---|---|---|
| `full_ft_qwen3_vl_4b_webcode2m/..._s42_20260731-164832/` | full-FT, финальные веса 9 ГБ | веса оставлены |
| то же, `checkpoint-{82,164,246}` | промежуточные, по 67 ГБ с состоянием оптимизатора | **удалены** (201 ГБ) |
| `merged/merged-checkpoint-{84,94,168,188,252,282}` | слитые LoRA, по 8.3 ГБ | **удалены** (50 ГБ), восстановимы из адаптеров |
| `sft-output/lora_ft_qwen3_vl_4b_webcode2m_val/` | сами LoRA-адаптеры, 2.6 ГБ | оставлены |
| `compare/`, `compare_fullft/` | картинки сравнения, по 8.5 МБ | оставлены |
| `train_*.log`, `after_train.log` | логи обучения | оставлены |
| `compare.py`, `diag.py`, `websight_h.py` | скрипты того периода | оставлены |

## Конфиги

Восстанавливаются из `run_info.yaml` рядом с весами и из `train_*.log`.
Модель — Qwen3-VL-4B, данные — WebCode2M в drafting-формате.

## Выводы

Ничего, что не перекрыто пилотом (см. [пилот 1k](2026-08-03-pilot-1k.md)):
та же связка «WebCode2M + агрессивный рецепт», но без измеримых чисел.
Ценность архива — только логи обучения и картинки сравнения.
