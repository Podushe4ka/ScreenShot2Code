# Пилот на 1000 примерах WebCode2M — E0…E6 (3 августа 2026)

**Статус:** завершён. Числа сняты **сэмплингом T=1.0** (см. оговорку внизу).

## Вопрос

LoRA или full-FT, и какой LR. Плюс две изолированные оси: разрешение входа
и ёмкость адаптера.

## Конфиг (общий для E1–E6)

Qwen3.5-4B, датасет `webcode2m_1000_split`, `max_length` 16384, **1 эпоха**,
микробатч 4 × accum 2 × 2 карты = эфф. батч 16 → 62 шага оптимизатора,
seed 42, машина a100-3, карты 0 и 3. Обучение прошло у всех (`rc=0`).

| ID | Метод | LR | Пиксель-бюджет | Что изолирует |
|---|---|---|---|---|
| E1 | LoRA r16 | 1e-4 | 2.10 Мп | референс |
| E2 | LoRA r16 | 3e-4 | 2.10 Мп | верхний край LR для LoRA |
| E3 | full-FT | 1e-5 | 2.10 Мп | нижний край LR для full-FT |
| E4 | full-FT | 3e-5 | 2.10 Мп | верхний край LR для full-FT |
| E5 | LoRA r16 | 1e-4 | **3.93 Мп** | ось A: разрешение (отличие от E1 только в бюджете) |
| E6 | LoRA **r64** | 1e-4 | 2.10 Мп | ось C: ёмкость (отличие от E1 только в ранге) |

## Результаты (Design2Code, 484 сэмпла, `max_new_tokens` 16384)

| ID | final | block | text | position | color | clip | eval_loss |
|---|---:|---:|---:|---:|---:|---:|---:|
| **E0 база** | **0.810** | 0.805 | 0.929 | 0.748 | 0.794 | 0.878 | — |
| E1 | 0.640 | 0.594 | 0.846 | 0.584 | 0.599 | 0.839 | 0.4662 |
| E2 | 0.647 | 0.601 | 0.837 | 0.586 | 0.618 | 0.853 | 0.4353 |
| E3 | 0.690 | 0.650 | 0.872 | 0.634 | 0.663 | 0.849 | 0.3847 |
| E4 | 0.503 | 0.438 | 0.665 | 0.466 | 0.502 | 0.883 | 0.3857 |
| E5 | 0.615 | 0.542 | 0.823 | 0.569 | 0.601 | 0.841 | 0.4767 |
| E6 | 0.663 | 0.626 | 0.851 | 0.603 | 0.621 | 0.847 | 0.4354 |

Отдельно лежат замеры на `max_new_tokens` 8192 (`E*-bench-8192tok`) —
устаревшие, там генерация обрезалась примерно у 40% сэмплов.

## Где лежит

`/mnt/storage-1/Screenshot2Code/checkpoints_exps/exps-20260803-104549/`

```
E<N>/<config>_s42_<дата>/   веса (LoRA-адаптер или полные)
E<N>-bench/summary.json     метрики
logs/E<N>.train.log         лог обучения + meta с пиксель-бюджетом
REPORT.txt, REPORT-bench.txt, README.md
```

Удалено 6 августа: промежуточные чекпоинты E3/E4 (шаги 20/40/59), все
`E*-merged/`, веса E4. Финальные веса E1/E2/E3/E5/E6 и все `summary.json` на месте.

ClearML: проект `Screenshot2Code`, тег `pilot1k`.

## Выводы

1. **Базу не побил никто.**
2. full-FT > LoRA; лестница ёмкости r16 < r64 < full подтвердилась (E1/E6/E3).
3. LR для full-FT критичен: 1e-5 ок (E3 — лучший FT), 3e-5 катастрофа (E4).
4. Гипотезы «ранг r64» и «пиксель-бюджет 3.93 Мп» — **обе провалились**.
5. Держатся `text` и `clip`, рушатся `block_match`/`position`/`color`:
   портится структура и раскладка, не контент.
6. ⚠ `clip` вводит в заблуждение: у сломанных сетапов растёт при обвале остального.

## ⚠ Оговорка (обнаружено 6 августа)

Все эти числа сняты при **temperature 1.0, top_p 1.0, без seed** — бенч не задавал
`SamplingParams` вовсе и получал дефолты vLLM. Относительные выводы, скорее всего,
устоят, но абсолютные значения надо пересчитывать в greedy.
См. [перепроверку greedy](2026-08-06-greedy-recheck.md).
